info:
  name: feature2
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/f2?userId=geoff@muzz.com"
  params:
    - name: userId
      value: geoff@muzz.com
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​